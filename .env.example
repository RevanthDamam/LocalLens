# Node.js API base path. Keep this blank to use the Vite `/api` proxy during local development.
# For a separate production API, set a complete URL such as https://api.example.com/api.
VITE_API_BASE_URL=

# Optional: Ollama URL for AI chat (defaults to same origin proxy)
# VITE_OLLAMA_URL=http://localhost:11434
