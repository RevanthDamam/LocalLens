# CornerStores Verification Notes

The direct repository production build completed successfully after the React redesign and API migration. The Node.js sources in `backend/src` also completed syntax validation with `node --check`.

Browser verification confirmed that the redesigned home page, discovery workspace, full-map route, and merchant-access route render without a client-side crash. The map loads its Leaflet base layer and category controls correctly.

The deployed Node API now uses the connected Supabase project’s Auth service and PostgreSQL-backed data API, so a direct database password, custom JWT secret, and bespoke schema migration are neither required nor stored in the repository. Merchant writes remain protected by Supabase sessions and the existing row-level security policies.

The root Vercel configuration installs both workspaces, builds `frontend/dist`, and routes `/api/*` to the Node serverless adapter before the SPA fallback. This keeps production React routes, merchant routes, and API responses on the same origin.

The active Vercel Git project was refreshed and the current `main` build was verified. The deployed static frontend initially received Vercel's HTML fallback on `/api/shops`, which made the prior response parser set an undefined shop collection and emptied the React root. The frontend API client now rejects non-JSON API responses and enforces array-shaped shop and item collections. A direct Vercel production deployment (`dpl_4YkxAwnznkBUNvzvA8Gd66KzqfCw`) then updated `https://local-lens-nu.vercel.app` successfully. Browser verification confirmed that the redesigned CornerStores field-guide home interface is now visible on the public URL.

The connected Supabase account confirmed the active CornerStores project at `skquddkyggertfdstdxm` with healthy PostgreSQL-backed `profiles`, `shops`, and `shop_items` tables. To preserve the existing Supabase Auth constraints, the Node API now uses Supabase Auth plus the supported Supabase data client rather than directly altering `auth.users`. Vercel preview deployment `dpl_9XYa233EmtjG3DukWvBiq83UXEiQ` returned `{"status":"ok","database":"supabase-postgres"}` from `/api/health` and returned the current CornerStores shop records from `/api/shops`.

Production deployment `dpl_dBT1saGiduFkDVDPM7VkJg6RXHJ7` is now active on `https://local-lens-nu.vercel.app`. The public `/api/health` route returns `{"status":"ok","database":"supabase-postgres"}`. The public `/api/shops?category=Bakery` route returns the corresponding live CornerStores record from the connected database, confirming that the React frontend's same-origin `/api` client is wired to the deployed Node API and Supabase PostgreSQL data layer.

After the production API response settled in the browser, the CornerStores home page rendered a live count of two listed places and displayed the live `wqeasd` and `The Flower Shop` cards. This confirms the deployed React frontend is consuming the production Node API rather than its prior empty fallback state.

The approved CornerStores branding update was deployed to the existing Vercel production project as deployment `dpl_55YAYkTA9N82SnivDasDLXFCH7XS`. It reached `READY` state and was assigned the existing `local-lens-nu.vercel.app` production alias. Browser verification confirmed the public page title, navigation, and application copy now display `CornerStores` while the established production URL remains unchanged.

Merchant listings now include a map-first location picker. An authenticated merchant can enter a full address or area query, explicitly submit it for address search, choose one returned result to set the address and center the map, then click the map to refine the final latitude and longitude. The form continues to submit the existing `address`, `latitude`, and `longitude` shop fields, so no database migration or shop API contract change was required. The production deployment `dpl_EXkccPMMR1Zq8UaMLeB3XH88jKdW` reached `READY` with the existing production aliases. Its `/api/locations/search` endpoint is reachable and correctly returns `401` without a merchant session, confirming it is protected by the existing authentication middleware.

The merchant selector was refined to a fixed center-pin model. Address search moves the Leaflet map to the selected area; merchants then pan or zoom beneath a persistent center pin, and the final centered coordinates are saved after map movement finishes. The replacement production deployment `dpl_64EzKQXsLZFveDCdRfpjT8dGd7Kw` reached `READY` and retains `local-lens-nu.vercel.app`. A production-bundle check confirmed the deployed merchant listing code includes the center-pin instructions and move-to-update behavior.

The structured location search now collects country, state or region, city, and area in four inputs above the map. Its `Find area` control is a non-submitting button, and Enter inside any location input prevents the enclosing shop form from submitting. The top result immediately moves the map to the matched area, while the result list allows a merchant to select a different match. Production deployment `dpl_4TpYQRC2QAZ7LC99ZKCgNLnk7s18` reached `READY` with the existing alias, and its client bundle contains the structured field labels and explicit non-submitting search guidance.

The focused cleanup retired unreachable template UI modules, the unused browser-side Supabase client, redundant Radix toast system, direct-Postgres pool and migration runner, and unused package dependencies. The runtime API now disables its technology header, applies strict JSON parsing, returns generic mapped database errors, rate-limits registration and login, sends no-store headers for account responses, validates public search input, and emits baseline browser security headers. React Router was upgraded to a patched release. Runtime dependency audits for both the frontend and backend returned zero reported vulnerabilities.

The final cleanup deployment `dpl_HATfCUv4dpaSH8i5hU1vHrpiRxsp` reached `READY` and retains `local-lens-nu.vercel.app`. Production header checks confirmed CSP, frame protection, no-sniff, referrer, and permissions policies; `/api/health` remained available. The public home page rendered normally in the browser with no console output after the CSP was applied.

After cleanup, production `GET /api/shops?category=Bakery` still returned one live Supabase-backed shop (`wqeasd`), while unauthenticated `GET /api/locations/search` returned `401` as intended. Local runtime checks also confirmed no-store headers on authentication endpoints and a `429` response after the configured registration rate-limit threshold. These checks confirm public discovery, authenticated merchant boundaries, and the existing map-search route were preserved.

The production category index now visibly renders thirteen icon-led categories: Bakery, Grocery, Cafe, Florist, Artisan, Restaurant, Electronics, Pharmacy, Fashion, Books & Stationery, Home & Living, Health & Beauty, and Pet Supplies. The catalog is the single source of truth for the category index, discovery filters, merchant selector, sample listings, and map-marker treatments.

The deployed map settled with fifteen mapped places: the existing live records plus one clearly labeled sample listing for each category. Its Leaflet markers now use category-specific color and symbol treatments, while the map filter shows the full thirteen-category catalog.

Filtering the live production register to Pet Supplies returned exactly one result, `Pet Supplies sample listing`. Its card visibly showed the category, `Sample` label, demonstration description and map point, `Unrated`, and no price claim. This confirms sample data is represented in the UI without being written to the merchant database or represented as verified business, review, rating, price, or availability information.
