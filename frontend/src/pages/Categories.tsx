/** Cartographic Editorial: the category index uses the same icon language as shop cards and map pins. */
import { ArrowUpRight, Shapes } from "lucide-react";
import { Link } from "react-router-dom";
import { CATEGORY_CATALOG } from "@/data/catalog";

export default function Categories() {
  return <main className="contour-surface min-h-[calc(100vh-70px)] px-5 py-12 sm:px-8 lg:px-10 lg:py-16"><div className="mx-auto max-w-[1440px]"><p className="atlas-label text-primary">The index / all categories</p><div className="mt-4 flex flex-wrap items-end justify-between gap-6 border-b border-border pb-7"><div><h1 className="max-w-2xl font-display text-5xl leading-[0.9] tracking-[-0.05em] sm:text-6xl">Find a starting point.</h1><p className="mt-5 max-w-xl text-sm leading-6 text-muted-foreground">Each category has its own field mark, making it easier to scan the register and recognize its matching places on the map.</p></div><Shapes className="h-11 w-11 text-primary/45" /></div><div className="mt-8 grid gap-px border border-border bg-border sm:grid-cols-2 lg:grid-cols-3">{CATEGORY_CATALOG.map(({ name, Icon, color, tint }, index) => <Link key={name} to={`/explore?category=${encodeURIComponent(name)}`} className="group min-h-48 bg-card p-5 transition hover:-translate-y-0.5 hover:shadow-xl"><div className="flex items-start justify-between"><span className="atlas-label text-muted-foreground">{String(index + 1).padStart(2, "0")}</span><span className="grid h-11 w-11 place-items-center rounded-full transition group-hover:scale-110" style={{ color, backgroundColor: tint }}><Icon className="h-5 w-5" strokeWidth={2.25} /></span></div><h2 className="mt-10 font-display text-3xl tracking-[-0.04em]" style={{ color }}>{name}</h2><p className="mt-2 flex items-center gap-2 text-xs font-bold text-muted-foreground">Open the category register <ArrowUpRight className="h-3.5 w-3.5 transition group-hover:-translate-y-0.5 group-hover:translate-x-0.5" /></p></Link>)}</div></div></main>;
}
